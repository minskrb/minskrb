<section class="show-photos">
  <div class="show-photos-content">
    <%= yield %>

    <div class="show-photos-content__photos">
      <% @event_images.each do |image| %>
        <%= image_tag photo(image) %>
      <% end %>
    </div>
  </div>
</section>
